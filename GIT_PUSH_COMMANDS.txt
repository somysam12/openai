===============================================
   GIT PUSH COMMANDS - Ab ye run karo
===============================================

# 1. Session file ko tracking mein lao
git add my_personal_account.session

# 2. Baaki changes add karo
git add .gitignore
git add personal_account_autoreply.py
git add replit.md
git add RENDER_PYROGRAM_DEPLOY.md
git add PYROGRAM_SETUP_HINDI.md

# 3. Commit karo
git commit -m "Fixed Pyrogram threading + Added session file for deployment"

# 4. Push karo
git push

===============================================
   RENDER ENVIRONMENT VARIABLES (Check karo)
===============================================

✅ TELEGRAM_BOT_TOKEN
✅ OPENAI_API_KEY (ya OPENAI_API_KEY_1, 2, 3...)
✅ ADMIN_ID
✅ TELEGRAM_API_ID          ← Ye add kiya?
✅ TELEGRAM_API_HASH        ← Ye add kiya?

===============================================
   RENDER SETTINGS
===============================================

Build Command:
    pip install -r requirements.txt

Start Command:
    python start_both_bots.py

Port: Auto (10000)

===============================================
   KYA HOGA DEPLOYMENT KE BAAD
===============================================

1. Main bot (TELEGRAM_BOT_TOKEN) - Chalu hoga ✅
2. Pyrogram personal bot - Chalu hoga ✅
3. Dono bots ek saath chalenge
4. Session file git se automatically load hogi
5. Personal DMs pe auto-reply milega

===============================================
   AGAR ERROR AAE TO
===============================================

"Conflict: terminated by other getUpdates"
    → Kisi aur jagah bot band karo (purana Render deployment)

"TELEGRAM_API_ID not set"
    → Environment variables Render pe check karo

Bot running but no response
    → Logs check karo, OpenAI API key working hai?

===============================================

BAS GIT PUSH KARO AUR RENDER AUTO DEPLOY KAREGA! 🚀
